package com.peakadex.app.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand blues (primary action colour everywhere in the app) ──────────────────
val PeakBlueActive    = Color(0xFF0369A1)   // sky-700  — interactive / primary
val PeakBlueLight     = Color(0xFF0EA5E9)   // sky-500  — gradient accent
val PeakBlueContainer = Color(0xFFDBEAFE)   // sky-100  — primaryContainer / progress track
val PeakBlueDark      = Color(0xFF0C4A6E)   // sky-900  — onPrimaryContainer

// ── Brand navies ───────────────────────────────────────────────────────────────
val PeakNavyDark      = Color(0xFF0D2538)   // body text / headlines
val PeakNavyMid       = Color(0xFF5A6E84)   // secondary text
val PeakNavyLight     = Color(0xFF94A3B8)   // disabled / hint

// ── Brand greens (secondary / CTA) ────────────────────────────────────────────
val PeakGreenCTA      = Color(0xFF2F7A5F)
val PeakBrandGreen    = Color(0xFF4A8C5C)

// ── Neutral surfaces ───────────────────────────────────────────────────────────
val PeakBackground    = Color(0xFFF4F7FA)
val PeakSurface       = Color(0xFFFFFFFF)
val PeakSurfaceVariant= Color(0xFFF3F4F6)   // dividers / inactive track
val PeakBorderLight   = Color(0xFFE5E7EB)

// ── Semantic / content colours ─────────────────────────────────────────────────
val PeakMuted         = Color(0xFF6B7280)   // gray-500 — secondary text
val PeakSubtle        = Color(0xFF9CA3AF)   // gray-400 — tertiary / hint text
val PeakOnSurface     = Color(0xFF374151)   // gray-700 — primary text on cards

// ── Achievement accent ─────────────────────────────────────────────────────────
val PeakAmber         = Color(0xFFF59E0B)   // amber-400 — earned badge dot
val PeakAmberContainer= Color(0xFFFEF3C7)   // amber-100 — earned badge background
val PeakAmberDark     = Color(0xFF92400E)   // amber-800 — earned badge text

// ── Atlas / map colours ────────────────────────────────────────────────────────
val PeakSlate         = Color(0xFF1E293B)   // slate-800  — map ctrl dark bg / body text
val PeakTextHeadline  = Color(0xFF111827)   // gray-900   — panel titles
val PeakClimbedGreen  = Color(0xFF22C55E)   // green-500  — climbed status dot / default rarity ring
val PeakUnclimbedBlue = Color(0xFF3B82F6)   // blue-500   — unclimbed peak markers
val PeakLayerActiveBg = Color(0xFFEFF6FF)   // blue-50    — active layer card background
val PeakSurfaceAlt    = Color(0xFFF9FAFB)   // gray-50    — inactive layer card background
